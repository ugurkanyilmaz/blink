/*
  Video controller (minimal REST helpers for signaling and token issuance)

  Planned responsibilities:
  - token issuance (TURN/STUN credentials)
  - status endpoints or logging for call sessions
  - heavy lifting (offer/answer/ice) will be handled in sockets via WebRTC signaling messages
*/

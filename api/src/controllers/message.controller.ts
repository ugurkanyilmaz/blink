/*
  Message controller

  Planned responsibilities:
  - sendMessage: validate, persist, emit via sockets
  - fetchConversation: paginate messages
  - unreadCount: return counts grouped by conversation
*/

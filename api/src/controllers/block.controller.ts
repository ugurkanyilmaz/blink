/*
  Block controller

  Planned responsibilities:
  - blockUser(userId, targetId): persist block relation
  - unblockUser(userId, targetId)
  - ensure blocked users do not appear in match or message results
*/

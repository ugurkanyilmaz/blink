/*
  Ads management routes

  Planned endpoints (examples):
  - GET /ads -> fetch active ads for a user
  - POST /ads -> create or schedule an ad (admin)
  - GET /ads/stats -> ad impressions/conversions (admin)
*/

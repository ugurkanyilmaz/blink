/*
  Rate limiter middleware

  Planned responsibilities:
  - Apply per-IP or per-user rate limiting to protect endpoints
  - Use Redis for distributed rate limiting when configured
  - Expose config for different limits per-route
*/
